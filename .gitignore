# Zig build artifacts
zig-out/
.zig-cache/
zig-cache/

# Secrets and runtime state (would only end up here if someone copied them off the phone)
.hp-server-creds.txt
.hp-server-creds.txt.tmp
.hp-server-blocklist.txt
.hp-server-blocklist.txt.tmp
*-creds.txt
*-blocklist.txt

# Cloudflare tunnel credentials
.cloudflared/
cert.pem
*.tunnel.json
b63b2262-*.json

# Append-only data (visits, uptime, logins)
data/
*.jsonl

# Environment files (any naming)
.env
.env.*
*.env
!.env.example
.hp-server.env
.hp-server-*.env

# Editor / OS
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example

# Node leftovers (project no longer uses Node, but in case anyone scaffolds something)
node_modules/
npm-debug.log
yarn-error.log

# Build/temp
*.tmp
*.bak
*.log

# Zig package fetch cache (committed by mistake earlier)
zig/hp-server/zig-pkg/

# Local commit message scratch file (created by my workflow, not source)
msg.txt
.commit-msg.tmp

# Local PowerShell helper scripts (Kiro session scratch, never source of truth)
*.ps1
!cli/*.ps1
unblock.sh
gen-key.sh
diag-*.sh
